import styled from 'styled-components';

export default styled.div`
  position: absolute;
  display: block;
  border: 0px;
  margin: -1px;
  padding: 0px;
  height: 1px;
  width: 1px;
  clip: rect(0, 0, 0, 0);
  overflow: hidden;
`;
